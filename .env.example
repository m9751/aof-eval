# aof-eval — required runtime env (Server Components fetch the eval schema SSR-only)
# The anon key is PUBLIC-by-design (read-only on the eval schema). NEVER put a service_role key here.
# Both vars use the NEXT_PUBLIC_ prefix (read in lib/supabase.ts, app/page.tsx, app/api/*).
NEXT_PUBLIC_SMOKIN_OPS_URL=https://xuvdcygqyuajtlpavafr.supabase.co
NEXT_PUBLIC_SMOKIN_OPS_ANON_KEY=
